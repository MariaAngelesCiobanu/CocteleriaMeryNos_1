package com.example.cocteleriamerynos

import android.os.Bundle
import androidx.recyclerview.widget.GridLayoutManager
import androidx.recyclerview.widget.RecyclerView
import com.example.cocteleriamerynos.databinding.ActivityMainBinding
import java.time.Instant
/*
class Cocktelerias {
private lateinit var binding: ActivityMainBinding

    super.onCreate(savedInstantState)
    binding = ActivityMainBinding.inflate(layoutInflater)
}
    override fun onCreate(savedInstanceState: Bundle?)
    {
        super.onCreate(savedInstanceState)
        binding = ActivityMainBinding.inflate(layoutInflater)
        setContentView(binding.root)

        val Cocktail = listOf<Cocktail>(
            Cocktail(
                "Martini(Dry)",
                listOf("Ginebra", "Vermouth Dry", "Olive(impar)")
                10,
                "Es un cocktail refrescado y clasico que contiene ginebra, vermút seco y para la decoración una o tres aceitunas, es bastante fuerte en cuanto a sabor alcoholico")
            )
            Cocktail(
                "Manhattan",
                listOf("Vermouth Rosso", "Angostura","Whisky Bourbon" ),
                10,
                "Es un cocktail refrescado que contiene vermút rosso(dulce/rojo), angostura y Whisky Americano Bourbon"
            )
            Cocktail(
                "Whisky Sour",
                listOf("Whisky", "Limon", "Angostura","clara de huevo(opcional)","sirope de azucar")
                10,
                "Es un cocktail clásico proveniente de la familia de los sour, contiene: whisky, limon, angostura para decorar, clara de huevo(para la creacion de la espuma(es opcional))y sirope de azucar",

            )

        binding.cocktailList.layoutManager =
            GridLayoutManager(this, 1, RecyclerView.VERTICAL, false)

        binding.booksRecyclerView.adapter = BookAdapter(
            books, this
        )
        }*/


